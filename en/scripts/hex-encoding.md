---
title: Encode and decode hexadecimal string
desc: The command can encode or decode a hexadecimal string.
img:
   hex-encoding: scripts/hex-encoding.png
html:
   hex-encoding: '<img src="%img.hex-encoding%" style="margin: 1em 1em;"/>'
---
# Hex Encoding

The **Hex Encoding** command encodes bytes into a hexadecimal string or decodes a hexadecimal string into a sequence of bytes.

%html.hex-encoding%

**Variable Name**  
Specify the name of the variable that contains the source string.

**Encoding**  
Specify the type of conversion.

* *From bytes to hexadecimal string*
* *From hexadecimal string to bytes*

**Result variable**  
Specify the name of the variable to which the result will be assigned. If no variable name is specified, the resulting string will be assigned to the original variable.
